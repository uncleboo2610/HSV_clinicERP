export interface IMedicineTable {
    drugName: string,
    price: number,
    unit: string,
    typeDrugName: string,
    quantity: number
}

export interface IPharmaceuticalWarehouse {
    drugName: string,
    drugId: number,
    price: number,
    unit: string,
    typeDrugName: string,
    quantity: number
}